<table class="table table-hover table-striped table-bordered">
  <thead>
    <tr>
      <th class="text-center">Họ tên</th>
      <th class="text-center">Username</th>
    </tr>
  </thead>

  <tbody>
    {{#each admins}}
      <tr>
        <td class="text-center">{{this.name}}</td>
        <td class="text-center">{{this.username}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>

<div class="center mb-2 mt-3">
    <nav aria-label="Page navigation example">
        <ul class="pagination">
            <li class="page-item {{#unless this.isPreviousPage}} disabled {{/unless}}">
                <a class="page-link " href="/admin/list-admin/{{this.previousPage}}" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li>
            {{#each pages}}
                <li class="page-item {{#if this.isCurrentPage}} active {{/if}}">
                    <a class="page-link" href="/admin/list-admin/{{this.page}}">{{this.page}}</a>
                </li>
            {{/each}}

            <li class="page-item {{#unless this.isNextPage}} disabled {{/unless}}">
                <a class="page-link" href="/admin/list-admin/{{this.nextPage}}" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li>
        </ul>
    </nav>
</div>